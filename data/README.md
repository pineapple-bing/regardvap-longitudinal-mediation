# Data are not included

This repository must not contain participant-level REGARD-VAP data.

Place authorised local files in this directory only for local execution. The
`.gitignore` prevents common clinical-data formats from being committed, but
every staged file must still be reviewed before pushing to a remote repository.

Required analysis inputs are documented in `config.example.R` and
`docs/analysis_plan.md`.
