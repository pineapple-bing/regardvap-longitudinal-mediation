# Executable scripts

Run `00_run_regardvap_manuscript_pipeline_no_wgs.R` after setting local paths.
It reproduces Sections 1--3 and creates local outputs only. No participant data
or generated results belong in Git.
